[User]
  name = USER_NAME
  email = USER_EMAIL

[github]
  user = GITHUB_USER
  token = GITHUB_TOKEN

[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto

[alias]
  b = branch
  ba = branch -a
  br = branch -r
  ci = commit -a
  co = checkout
  d = diff
  info = config --list
  st = status
  up = pull --rebase
  cat = cat-file -p

[apply]
  whitespace = nowarn

[core]
  editor = vim
  pager = cat
  excludesfile = $HOME/.gitignore

[branch "master"]
  remote = origin
  merge = refs/heads/master
